using UnityEngine;

namespace Timeline.Extra
{
    // Helper attribute that removes the fold out and draws all child properties inline.
    public class NoFoldOutAttribute : PropertyAttribute
    {
    }
}
